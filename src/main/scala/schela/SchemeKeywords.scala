package schela

object SchemeKeywords {
  val quote: List[Char] = "quote".toList
  val `if`: List[Char] = "if".toList
  val `set!`: List[Char] = "set!".toList
  val define: List[Char] = "define".toList
  val lambda: List[Char] = "lambda".toList
}
